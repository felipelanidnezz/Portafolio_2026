services:
  - type: web
    name: ouija-ai-felipe
    runtime: node
    rootDir: ouija-ai
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: "20"
